# System Dependencies

## Linux (Ubuntu/Debian)
```bash
sudo apt install libdbus-1-dev libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libssl-dev libudev-dev
```

## macOS
No additional system dependencies required (uses built-in frameworks).

## Windows
No additional system dependencies required.
